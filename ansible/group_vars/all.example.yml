# Copy to ansible/group_vars/all.yml and fill in.
# Better: use SOPS — see all.sops.example.yaml for the encrypted workflow.

# --- Offsite S3 Backup (friend's MinIO server) ---
s3_endpoint: "https://s3.your-offsite.example.com"
s3_access_key: "your-access-key"
s3_secret_key: "your-secret-key"
s3_bucket: "homelab-lxc-backups"

http:
  routers:
    forgejo-native:
      rule: "Host(`git.isarcloud.eu`)"
      service: "forgejo-svc"
      entryPoints: ["websecure"]
      tls:
        certResolver: letsencrypt
    # SSO-protected endpoint. Keep native endpoint above as a break-glass fallback.
    forgejo-authentik:
      rule: "Host(`git-auth.isarcloud.eu`)"
      service: "forgejo-svc"
      entryPoints: ["websecure"]
      middlewares: ["authentik-forward-auth"]
      tls:
        certResolver: letsencrypt
  middlewares:
    authentik-forward-auth:
      forwardAuth:
        address: "http://10.10.20.54:9000/outpost.goauthentik.io/auth/traefik"
        trustForwardHeader: true
        authResponseHeaders:
          - X-authentik-username
          - X-authentik-groups
          - X-authentik-email
          - X-authentik-name
          - X-authentik-uid
  services:
    forgejo-svc:
      loadBalancer:
        servers:
          # Pointing directly to the new Forgejo LXC on VLAN 20
          - url: "http://10.10.20.100:3000"

http:
  routers:
    adguard:
      rule: "Host(`dns.isarcloud.eu`)"
      service: "adguard-svc"
      entryPoints: ["websecure"]
      tls:
        certResolver: letsencrypt
  services:
    adguard-svc:
      loadBalancer:
        servers:
          - url: "http://10.10.20.99:3000"

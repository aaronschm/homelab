---
- name: Configure Traefik Reverse Proxy in DMZ LXC
  hosts: traefik_lxc
  become: yes
  vars:
    traefik_version: "3.0.4"
    traefik_acme_email: "business@aaronschm.de"
  tasks:
    - name: Ensure required directories exist
      file:
        path: "{{ item }}"
        state: directory
        mode: '0755'
      loop:
        - /etc/traefik
        - /etc/traefik/dynamic
        - /etc/traefik/ssl
        - /var/log/traefik
        - /opt/traefik

    - name: Download Traefik binary
      unarchive:
        src: "https://github.com/traefik/traefik/releases/download/v{{ traefik_version }}/traefik_v{{ traefik_version }}_linux_amd64.tar.gz"
        dest: /opt/traefik
        remote_src: yes
        creates: /opt/traefik/traefik

    - name: Check installed Traefik launcher path
      stat:
        path: /usr/local/bin/traefik
      register: traefik_launcher_stat

    - name: Remove conflicting Traefik binary before linking
      file:
        path: /usr/local/bin/traefik
        state: absent
      when:
        - traefik_launcher_stat.stat.exists
        - not traefik_launcher_stat.stat.islnk

    - name: Create symlink
      file:
        src: /opt/traefik/traefik
        dest: /usr/local/bin/traefik
        state: link
        force: true

    - name: Create static configuration
      copy:
        dest: /etc/traefik/traefik.yml
        content: |
          entryPoints:
            web:
              address: ":80"
              http:
                redirections:
                  entryPoint:
                    to: websecure
                    scheme: https
            websecure:
              address: ":443"
          
          providers:
            file:
              directory: "/etc/traefik/dynamic"
              watch: true
          
          api:
            dashboard: true
            insecure: false
          
          log:
            level: INFO
            
          certificatesResolvers:
            letsencrypt:
              acme:
                email: "{{ traefik_acme_email }}"
                storage: "/etc/traefik/ssl/acme.json"
                httpChallenge:
                  entryPoint: web

    - name: Ensure acme.json exists with correct permissions
      file:
        path: /etc/traefik/ssl/acme.json
        state: touch
        mode: '0600'

    - name: Create systemd service for Traefik
      copy:
        dest: /etc/systemd/system/traefik.service
        content: |
          [Unit]
          Description=Traefik Reverse Proxy
          After=network-online.target
          Wants=network-online.target systemd-networkd-wait-online.service

          [Service]
          Restart=on-abnormal
          ExecStart=/usr/local/bin/traefik --configfile=/etc/traefik/traefik.yml
          LimitNOFILE=1048576
          PrivateTmp=true
          PrivateDevices=false
          ProtectHome=true
          ProtectSystem=full
          ReadWritePaths=/etc/traefik /var/log/traefik
          CapabilityBoundingSet=CAP_NET_BIND_SERVICE
          AmbientCapabilities=CAP_NET_BIND_SERVICE
          NoNewPrivileges=true

          [Install]
          WantedBy=multi-user.target

    - name: Enable and start Traefik service
      systemd:
        name: traefik
        state: started
        enabled: yes
        daemon_reload: yes

    - name: "Sync local dynamic config folder to Traefik LXC"
      copy:
        src: traefik-dynamic/
        dest: /etc/traefik/dynamic/
        owner: root
        group: root
        mode: '0644'
        directory_mode: '0755'

    - name: Remove stale conf.d rules from previous Traefik layout
      file:
        path: /etc/traefik/conf.d
        state: absent

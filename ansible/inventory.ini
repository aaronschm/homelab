[udm]
udmpro ansible_connection=local

# The UDM Pro is reached over its HTTPS API, not SSH — we use ansible_connection=local
# and the community uri module to talk to https://<udm_host>. Set udm_host in group_vars.

# --- INFRASTRUCTURE CONFIG ---
UPDATE_SERVER_IP="10.10.20.100"
REGISTRY_IP="10.10.20.101"
ADMIN_LXC_IP="10.10.20.20"
LOAD_BALANCER_IP="10.10.20.21" # The IP the Agents will talk to
DMZ_REVERSE_PROXY_IP="10.10.24.10"
GAMESERVER_IP="10.10.24.20"

# --- K3S CONFIG (legacy / traditional method only) ---
CONTROL_PLANE_IPS="10.10.25.11"
AGENT_IPS="10.10.25.101"
# SECURITY: never commit a real token. Generate one and export it at runtime:
#   export K3S_TOKEN="$(openssl rand -hex 32)"
# The Talos-based flow (infrastructure/terraform) does not use this token.
K3S_TOKEN="${K3S_TOKEN:-CHANGE_ME_GENERATE_AT_RUNTIME}"


apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: common
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/aaronschm/homelab.git
    targetRevision: main
    path: kubernetes/common
    directory:
      recurse: true
      exclude: "{**/*.example.yaml,**/*.sops.yaml,**/kustomization.yaml,**/*.md}"
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: router-dashboard-data
  namespace: router-dashboard
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
